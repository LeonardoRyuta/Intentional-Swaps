{
    "local": {
        "bitcoin": {
            "canister_init_arg": "(record { stability_threshold = opt 2; network = opt variant { regtest }; blocks_source = opt principal \"aaaaa-aa\"; syncing = opt variant { enabled }; fees = opt record { get_utxos_base = 0; get_utxos_cycles_per_ten_instructions = 0; get_utxos_maximum = 0; get_balance = 0; get_balance_maximum = 0; get_current_fee_percentiles = 0; get_current_fee_percentiles_maximum = 0; send_transaction_base = 0; send_transaction_per_byte = 0; } })",
            "enabled": true,
            "nodes": [
                "127.0.0.1:18444"
            ]
        },
        "bind": "127.0.0.1:8080",
        "type": "ephemeral",
        "replica": {
            "subnet_type": "system"
        }
    }
}